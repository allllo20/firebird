name: Build for WebAssembly + Deploy to GitHub Pages

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-wasm:
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ───────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # ── 2. Emscripten SDK (Qt 5.15.2 benötigt exakt 1.39.8) ──────────────
      - name: Install Emscripten 1.39.8
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: 1.39.8
          actions-cache-folder: emsdk-cache

      - name: Verify emcc
        run: emcc --version

      # ── 3. Qt 5.15.2 für WebAssembly ──────────────────────────────────────
      - name: Install aqtinstall
        run: pipx install aqtinstall

      - name: Install Qt 5 – Linux host tools (qmake, moc, rcc)
        run: aqt install-qt linux desktop 5.15.2 gcc_64 --outputdir $HOME/Qt

      - name: Install Qt 5 – WebAssembly target
        run: aqt install-qt linux desktop 5.15.2 wasm_32 --outputdir $HOME/Qt

      - name: Export Qt paths
        run: |
          echo "QT_HOST=$HOME/Qt/5.15.2/gcc_64"      >> $GITHUB_ENV
          echo "QT_WASM=$HOME/Qt/5.15.2/wasm_32"     >> $GITHUB_ENV
          echo "$HOME/Qt/5.15.2/gcc_64/bin"           >> $GITHUB_PATH
# ── 3.5. Patch source code for WASM compatibility ──────────────────────
      - name: Patch code – disable QThread::setTerminationEnabled for WASM
        run: |
          # Diese Funktion existiert nicht in Qt WASM, daher mit #ifndef EMSCRIPTEN auskommentieren
          find . -name "*.cpp" -o -name "*.h" | xargs grep -l "setTerminationEnabled" | while read file; do
            sed -i 's/\(.*setTerminationEnabled.*\)/#ifndef __EMSCRIPTEN__\n\1\n#endif/' "$file"
          done
          echo "Patched files for WASM compatibility"

      # ── 4. Build ──────────────────────────────────────────────────────────
      - name: Run qmake (WebAssembly)
        run: |
          mkdir build-wasm && cd build-wasm
          $QT_WASM/bin/qmake .. \
            -spec wasm-emscripten \
            "CONFIG+=release" \
            "TRANSLATION_ENABLED=false" \
            "QMAKE_CFLAGS+=-s USE_ZLIB=1" \
            "QMAKE_CXXFLAGS+=-s USE_ZLIB=1" \
            "QMAKE_LFLAGS+=-s USE_ZLIB=1"

      - name: Patch Makefile – fix missing -s prefix on Emscripten linker flags
        working-directory: build-wasm
        run: |
          # Qt 5.15.2 wasm mkspec erzeugt bare flags ohne -s (Bug mit Emscripten >= 2.x)
          sed -i \
            -e 's/\bWASM=1\b/-s WASM=1/g' \
            -e 's/\bFULL_ES2=1\b/-s FULL_ES2=1/g' \
            -e 's/\bFULL_ES3=1\b/-s FULL_ES3=1/g' \
            -e 's/\bUSE_WEBGL2=1\b/-s USE_WEBGL2=1/g' \
            -e 's/\bEXIT_RUNTIME=1\b/-s EXIT_RUNTIME=1/g' \
            -e 's/\bERROR_ON_UNDEFINED_SYMBOLS=1\b/-s ERROR_ON_UNDEFINED_SYMBOLS=1/g' \
            -e 's/\bEXTRA_EXPORTED_RUNTIME_METHODS=\[/-s EXTRA_EXPORTED_RUNTIME_METHODS=[/g' \
            -e 's/\bFETCH=1\b/-s FETCH=1/g' \
            Makefile
          echo "Patch applied."

      - name: Build
        working-directory: build-wasm
        run: make -j$(nproc)

      # ── 5. Ausgabe-Verzeichnis für Pages zusammenstellen ──────────────────
      - name: Assemble Pages directory
        run: |
          mkdir -p pages

          # Die von Qt WASM generierten Dateien
          cp build-wasm/firebird-emu.html   pages/index.html   || \
          cp build-wasm/firebird.html       pages/index.html
          cp build-wasm/*.js                pages/
          cp build-wasm/*.wasm              pages/
          # Optionale Dateien (existieren nur bei bestimmten Qt-Konfigurationen)
          cp build-wasm/*.data              pages/ 2>/dev/null || true
          cp build-wasm/*.worker.js         pages/ 2>/dev/null || true

          # COOP/COEP Headers via Service Worker (nötig für SharedArrayBuffer)
          cat > pages/coi-serviceworker.js << 'EOF'
          /* coi-serviceworker v0.1.7 – makeshift crossOriginIsolated for GitHub Pages */
          self.addEventListener("install",()=>self.skipWaiting());
          self.addEventListener("activate",e=>e.waitUntil(self.clients.claim()));
          self.addEventListener("fetch",e=>{
            if(e.request.cache==="only-if-cached"&&e.request.mode!=="same-origin")return;
            e.respondWith(
              fetch(e.request).then(r=>{
                if(r.status===0)return r;
                const h=new Headers(r.headers);
                h.set("Cross-Origin-Opener-Policy","same-origin");
                h.set("Cross-Origin-Embedder-Policy","require-corp");
                return new Response(r.body,{status:r.status,statusText:r.statusText,headers:h});
              })
            );
          });
          EOF

          # Inject Service Worker registration into index.html
          sed -i 's|<head>|<head>\n<script>if(!crossOriginIsolated){const r=new Promise(res=>navigator.serviceWorker.register("coi-serviceworker.js").then(res,res));if(!navigator.serviceWorker.controller)r.then(()=>location.reload());}</script>|' pages/index.html

          echo "Pages content:"
          ls -lh pages/

      # ── 6. GitHub Pages Upload ────────────────────────────────────────────
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages/

  deploy:
    needs: build-wasm
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
